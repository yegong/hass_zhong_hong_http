# 协作手册

本文档是 AI 与用户协作开发时的工作约定。目标是减少无效修改、保护已经确认的设计决策、让不确定性显式化，并把复杂度控制在正确的位置。

这些原则应适用于多数软件项目，包括脚本、后端服务、前端应用、桌面应用、嵌入式、自动化流程、数据处理和测试基础设施。项目中的具体经验可以作为示例，但不能替代通用原则。

## 核心原则

### 先理解系统，再修改代码

动手前先读相关代码、配置、文档和现有命名。不要只根据用户描述或局部错误信息直接 patch。

修改前至少要理解：

- 目标行为是什么；
- 现有实现如何分层；
- 哪些模块拥有相关状态；
- 哪些接口已经表达了类似语义；
- 哪些旧结构可能需要删除，而不是继续兼容。

对于小改动，可以在阅读后直接执行。对于结构性调整，应先明确边界和数据流。

### 在故障所在层级修复

问题出现在哪里，优先在对应层级定位和修复。不要为了让表面现象消失，把问题绕到其他层。

绕开故障点的修复通常会扩散问题，带来持久化的熵增。它看似降低了当下难度，实际是把复杂度藏到错误层级，未来会以更多分支、重复逻辑、奇怪命名和边界泄漏的形式返回。

典型反例：

- 构建系统没有识别新文件，就把实现塞进已有文件；
- 依赖注入配置错误，就让业务代码手动创建依赖；
- API 返回不稳定，就让 UI 层到处写解析补丁；
- 数据模型约束暴露问题，就在多个调用点散落前置判断；
- 测试难写，就删除边界或扩大 mock 到失去意义。

短期 workaround 可以存在，但必须被明确标注、限制范围，并说明后续清理条件。

### 推翻决策需要更高证据

架构边界、模块拆分、数据模型、命名体系、状态机、依赖方向等结构性决策一旦被确认，就不能因为实现中遇到局部阻力而随手推翻。

如果实现遇到障碍，默认先检查：

- 是否误解了原设计；
- 是否实现方式有问题；
- 是否配置、构建、测试、生成代码或运行环境有问题；
- 是否只是需要补充一个小的中性接口；
- 是否存在不改变架构的直接修复。

只有新事实证明原决策在目标、约束或事实层面错误，才进入重新设计。推翻已确认决策前，应说明：

- 原决策基于什么假设；
- 新事实推翻了哪个假设；
- 保留原决策的代价；
- 改变决策的迁移成本和长期影响；
- 新方案为什么比原方案更稳。

推翻结构性决策所需的思考投入，应明显高于当初做出该决策时的投入。

### 先确认边界，再做结构性修改

如果任务涉及架构、模块边界、跨层调用、状态归属或长期扩展点，不要从局部症状开始连续 patch。应先明确：

- 每个模块负责什么；
- 每个模块不应该知道什么；
- 数据和控制流如何穿过边界；
- 哪些接口是稳定语义，哪些只是内部实现；
- 哪些旧抽象需要删除、迁移或保留。

边界不清时，局部修改往往会形成“每一步都能解释，但整体越来越难维护”的结果。

## 模块与抽象

### 依赖稳定语义，不依赖内部表示

跨边界接口应该传递稳定语义，而不是当前实现里最方便的数据结构。

例如：

- UI 层应表达用户意图，不应依赖后端内部表结构；
- 业务层应依赖领域概念，不应依赖 HTTP、SQL、字节流、DOM 或文件格式细节；
- 调度器应依赖任务接口，不应知道每个具体任务的内部状态；
- 测试应验证行为契约，不应过度绑定临时实现细节。

内部表示一旦泄漏到外层，后续功能会沿着泄漏点扩散，最终让边界失效。

### 尊重所有权方向

组合关系、调度关系和拥有关系应保持方向清晰。被组合或被调度的对象通常不应该反向知道具体容器，除非这是明确设计。

优先使用：

- 小而稳定的 context/interface；
- 事件、回调或订阅接口；
- 依赖注入；
- 由上层组合、由下层暴露能力的结构。

当一个低层模块开始感知高层 owner、页面、控制器、流程或具体业务场景时，应重新检查边界。

### 底层能力应高于上层需求

理想的分层不是每一层能力完全对齐，而是底层更抽象、更稳定，能力覆盖真实领域和长期演进；上层更贴近产品、交互、接口和当前需求，因此可以更频繁地调整。

上层 API 可以为了易用性提供窄入口，例如单字段设置、单按钮动作、单页面表单提交、固定报表、特定页面交互。底层领域对象、服务、数据模型和流程能力不应因此被设计得同样狭窄。

底层模型应覆盖真实业务能力，而不是只贴合当前 UI、HA entity、HTTP endpoint、CLI 参数、设计稿或某个临时调用点。需求变化时，优先让上层编排、转换和适配；只有当底层抽象不能表达真实领域能力时，才修改底层。

如果上层每次需求变化都需要修改底层，通常说明底层抽象过窄、能力位置过高，或者缺少稳定的领域对象。

不要因为当前产品形态只暴露某种组合关系，就把底层概念强行绑定在一起。当前需求可能暂时掩盖错误边界，但后续一旦出现新的访问路径、展示形态、编辑能力、权限组合或自动化流程，早期绑定会让系统举步维艰，重构代价也会显著升高。

判断原则：

- 上层可以贴近当前需求，底层应服务长期演进；
- 上层 convenience API 可以窄，底层领域能力不应被便利入口限制；
- 上层可以频繁调整，底层应尽量稳定；
- 当前需求中的固定组合，不一定是底层领域中的必然绑定；
- 不要因为当前只有单项入口，就把底层设计成只能单项；
- 如果底层能力收得太窄，后续需求变化会反复牵动基础结构。

### 单一事实来源

同一规则、状态、常量、映射、格式、命令构造或业务判断不应分散复制。

如果必须重复，应明确这是缓存、派生值、兼容层还是展示层副本，并说明谁是事实来源。

重复事实来源会导致：

- 修改遗漏；
- 行为不一致；
- 测试覆盖困难；
- 用户看到的状态与系统真实状态不一致；
- 后续实现者无法判断应该改哪一处。

### 命名反映真实职责

命名不是装饰，它会影响后续修改者对职责的判断。

如果一个组件已经负责调度、缓存、解析、状态归并或错误恢复，就不要继续使用暗示它只是普通 helper、buffer、util、manager 的名字。

重构后，如果旧名字已经不能表达真实职责，应及时改名。错误命名会掩盖边界问题。

### 不继承来源系统的临时命名

旧系统、厂商接口、抓包记录、数据库字段、设计稿占位名、第三方 API、自动生成代码中的词，通常贴近来源系统的历史和内部实现。它们可以帮助定位字段，但不应无条件进入新的业务模型、对外 API 或 UI。

命名时应区分：

- 来源语言：外部系统、协议、数据库、旧代码、设计稿或日志中的名字；
- 分析语言：逆向、迁移或调试阶段的候选名；
- 业务语言：稳定、通用、可被其他模块理解的领域概念。

低置信、疑似冗余、镜像、兼容或实验字段，应使用清晰前缀或隔离在内部层，直到业务含义被证明。

## 实现策略

### 优先小而完整的改动

每次改动应尽量围绕一个清晰目标，包含必要的实现、清理和验证。不要把无关重构、格式化、命名迁移和功能改动混在一起。

小改动不是只改一行，而是影响面清楚、回滚点明确、验证方式直接。

### 删除过时抽象

不要因为旧类、旧常量、旧 helper 还能编译就保留它们。重复的抽象会让系统出现多个解释入口。

重构后应搜索：

- 旧类名；
- 旧方法名；
- 旧数据模型名；
- 旧注释；
- 已不符合新架构的中间层。

能删除就删除。不能删除时，应明确兼容原因和边界。

### 能工作不等于结构正确

代码短期能运行，不能证明职责划分正确。评估结构时应问：

- 新增类似功能是否会修改无关模块；
- 一个模块是否知道了不属于它的细节；
- 接口是否传递了稳定语义；
- 当前命名是否会误导下一个实现者；
- workaround 是否正在变成事实设计。

如果答案暴露结构问题，应优先修结构，而不是继续堆功能。

## 复杂度门槛

### 边缘兼容需要收益评估

边缘兼容逻辑不是越多越好。几行代码、边界清晰、不会扩散状态的小兼容可以直接实现。

如果为了一个低频场景需要引入以下内容，应先停下来说明并征求确认：

- 新状态机；
- 长期缓存或持久化；
- 跨层接口；
- 全局状态；
- 重试/回滚/补偿逻辑；
- 多模块协同；
- 大量分支；
- 对端新增多个专用接口。

说明时应包括：

- 这个兼容解决什么风险；
- 大约增加多少有效代码；
- 会引入哪些状态或跨层依赖；
- 不做这个兼容的实际后果；
- 是否存在更简单的产品约束或操作约束。

### 专用接口过多时重新审视边界

如果单个功能要求对端新增多个接口，或者新增接口看起来只服务于这个功能，应重新审视边界。

可能的问题包括：

- 功能放错层；
- 缺少更通用的领域对象；
- 当前接口过窄；
- 流程状态应该被抽成独立对象；
- 上层正在索取下层内部状态。

不要用一组零散接口把两个模块强行绑在一起。

## 构建、测试与工具问题

构建、测试、生成代码、依赖安装、缓存、锁文件、CI、IDE 索引等问题，优先按工具链问题处理。不要把工具链症状直接转化为源码架构修改。

处理顺序：

1. 确认错误发生在哪个层级；
2. 清理缓存或重新生成；
3. 检查配置、发现规则、依赖和环境；
4. 确认是否有权限、路径、版本或离线模式问题；
5. 必要时向用户说明工具链问题；
6. 只有证明源码结构本身错误，才调整源码结构。

这条原则不是要求死守错误设计，而是避免用未经分析的源码改动绕开工具链故障。

## 协作方式

### 用户质疑边界时暂停局部 patch

当用户指出“职责不对”“边界不对”“依赖方向不对”“这个接口太专用”“这个兼容太复杂”时，应把它视为重新审视设计的信号，而不是一个局部修正建议。

此时应暂停继续堆 patch，转为说明：

- 当前设计如何理解；
- 用户质疑可能指向哪个边界问题；
- 可选方案是什么；
- 每个方案的复杂度和长期影响；
- 推荐方案和原因。

### 架构类任务先给出设计说明

结构性任务中，AI 应先给出简短设计说明，再实现。说明应覆盖：

1. 目标行为；
2. 会修改哪些模块；
3. 各模块职责；
4. 数据和控制流；
5. 哪些旧结构会删除或保留；
6. 风险、回滚点和验证方式。

如果用户已经明确要求直接执行，且边界已经足够清楚，可以边实现边保持简短说明。

### 不确定性要显式化

如果信息来自推测、日志现象、逆向分析、第三方行为或不稳定环境，应明确标注置信度。不要把猜测写成事实。

可以使用：

- “已确认”；
- “倾向于”；
- “目前观察到”；
- “需要后续验证”；
- “这是为了兼容当前现象，不代表长期模型”。

### 先验证，再总结

完成代码修改后，应根据风险选择验证方式。至少应检查 diff；能运行配置、测试或编译时，应尽量执行。

总结时说明：

- 修改了什么；
- 为什么这样改；
- 验证结果；
- 未验证的风险；
- 是否已提交。

## 附录：案例

以下案例只用于说明原则如何落地，不是通用规则。

### 案例一：设备通讯模块的边界泄漏

一次 ESPHome external component 开发中，目标是实现设备通讯模块。最初没有先固定依赖方向，导致多轮反复：

- 业务协调类保存了本应属于任务层的时间状态；
- 任务层接触了本应属于协议层的原始帧结构；
- 协议层与任务层之间缺少清晰订阅接口；
- 常量和消息构造分散在多个层；
- parser 的名字低估了真实职责。

修正方向是重新划分层级：

- 业务协调层只保存业务状态并调度任务；
- 任务层维护业务请求、响应和时序；
- 协议层负责传输格式、封装和分发；
- 字节流解析只存在于协议内部；
- 跨层接口传递稳定语义，不泄漏内部表示。

这个案例对应的通用原则是：先确认边界，再做结构性修改；跨边界传递语义，不传递内部表示。

### 案例二：构建问题不应反向推翻源码结构

一次 ESPHome external component 重构中，新增独立 `.cpp` 文件后，首次编译链接失败。原因是增量构建没有重新识别 external component 的新源文件。

错误做法是：

- 把 helper 改成 header-inline；
- 把流程实现搬进已有大型源文件；
- 为了链接通过破坏刚确认过的模块边界。

正确做法是：

- 保留独立编译单元；
- clean 构建目录；
- 确认构建系统是否重新发现源文件；
- 必要时说明这是构建系统或缓存问题。

这个案例对应的通用原则是：在故障所在层级修复；推翻已有架构决策需要更高证据。
